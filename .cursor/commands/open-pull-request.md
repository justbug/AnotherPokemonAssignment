# Open Pull Request

## Overview
Follow the `.github/pull_request_template.md` template to create pull request.

## Steps
1. **Analyze Current Change** - Use `git diff` command to analyze changes between current branch and main branch
2. **Use GH CLI to Open Pull Request** - Always follow the template. Write clear, percise briefing about the changes